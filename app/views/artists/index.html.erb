<h1>Artists</h1>

<% @artists.each do |artist| %>

<p>
<!--<%= image_tag artist.image_url, :size => "100x100" %>-->
<%= link_to artist.name, artist_path(artist.id) %>
</p>
<% end %>

<hr>
<%= link_to "[New Artist]", new_artist_path %>
<%= link_to "[All Songs]", root_path %>
